package com.dagothig.knightfight.input;

public class KnightFightMappings {
    public static enum Button {
        JUMP,
        JOIN,
        START,
        THROW
    }

    public static enum Axis {
        LEFT_HORIZONTAL,
        LEFT_VERTICAL,
        LEFT_TRIGGER,
        RIGHT_HORIZONTAL,
        RIGHT_VERTICAL,
        RIGHT_TRIGGER
    }
}
